services:
  - type: web
    name: speaker-audio
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    autoDeploy: true
    envVars:
      - key: PYANNOTE_TOKEN
        sync: false
